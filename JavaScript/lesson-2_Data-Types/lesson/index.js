/* 
* JS data types 
* Tvyalneri tipery
*/

// Dinamic(tuyl tipavorvac lezu)(aysinqn wor popoxakan es stexcum ches nshum inch tipia inch tipi ases kara lini) & Static(hastatun(xist tipavorum))

// Primitive types -------------

// 1 number (..., NaN, infinity -infinity)
// 2 string
// 3 boolean(true, false)
// 4 undefined
// 5 null 


var a = 10;
var b = 230;

/**
 * Operators (tiper@)
 */

// 1. unar (+, -)
// 2. binar (+, -, *, /, %) (=, +=, -=, *=, /=, %=) (>, <, >=, <=, ==, !=, ===, !==), (&&, ||) 2 popoxakana uzum
// 3. ternar



// && ete dzax kam aj kom@ sxala uremn sxaala partadir piti 2 sne jisht ylni
//(true && false) = false
//(false && true) = false
//(false && false) = false


//(true || true) = true     KAM
//(true || false) = true
//(false || true) = true
//(false || false) = false